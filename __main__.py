import numpy as np


def submitRes(res):
  if res:
    print("Analysing Prompt")
    for l in length(res):
      pass
  else:
    print("It seems that you have not submited a prompt.")

i = input("> ")
submitRes(i)
